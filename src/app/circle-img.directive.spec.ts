import { CircleImgDirective } from './circle-img.directive';

describe('CircleImgDirective', () => {
  it('should create an instance', () => {
    const directive = new CircleImgDirective();
    expect(directive).toBeTruthy();
  });
});
